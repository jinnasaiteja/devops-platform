apiVersion: v1
kind: Service
metadata:
  name: devops-platform-service
spec:
  type: LoadBalancer
  selector:
    app: devops-platform-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000